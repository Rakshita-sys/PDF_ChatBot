# PDF_ChatBot
It is an intelligent chatbot application that allows users to upload PDF documents and interactively query their content. It processes the document, converts the text into embeddings using a language model, stores them in a vector database, and uses an advanced AI model to fetch context-aware answers based on the uploaded document.
